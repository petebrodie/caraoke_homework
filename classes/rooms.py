class Rooms:

    def __init__(self, room_number, check_in, check_out)
    self.room_number = room_number
    self.check_in_guest = check_in_guest
    self.check_out_guest = check_out_guest
    
def check_in_guest(self, name):
    self.name.append(name)

def check_out_guest(self, name):
    self.name.remove(name)

def add_songs_to_rooms(self):
    
    